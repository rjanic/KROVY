<Window x:Class="AcKrovy.AutoCAD.UI.LayerSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AcKrovy.AutoCAD.UI"
        xmlns:localization="clr-namespace:AcKrovy.AutoCAD.UI.Localization"
        xmlns:settings="clr-namespace:AcKrovy.Localization;assembly=AcKrovy.Localization"
        xmlns:automation="clr-namespace:System.Windows.Automation;assembly=PresentationCore"
        Title="{localization:Loc SettingsWindow_Title}"
        Width="1500"
        Height="900"
        MinWidth="1250"
        MinHeight="720"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource SettingsWindowBackgroundBrush}"
        Foreground="{DynamicResource SettingsTextPrimaryBrush}"
        FontFamily="{DynamicResource SettingsFontFamily}"
        FontSize="{DynamicResource SettingsFontSizeBody}">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Design/SettingsColors.Light.xaml" />
                <ResourceDictionary Source="Design/SettingsDesignSystem.xaml" />
                <ResourceDictionary Source="Design/SettingsControls.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style x:Key="SectionHostStyle" TargetType="Border" BasedOn="{StaticResource SettingsCardStyle}">
                <Setter Property="Visibility" Value="Collapsed" />
            </Style>
            <Style x:Key="LayersSectionHostStyle" TargetType="Border" BasedOn="{StaticResource SectionHostStyle}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Visual.SelectedSection}" Value="{x:Static settings:SettingsWindowTabKind.Layers}">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="ManufacturingSectionHostStyle" TargetType="Border" BasedOn="{StaticResource SectionHostStyle}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Visual.SelectedSection}" Value="{x:Static settings:SettingsWindowTabKind.Manufacturing}">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="AnnotationSectionHostStyle" TargetType="Border" BasedOn="{StaticResource SectionHostStyle}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Visual.SelectedSection}" Value="{x:Static settings:SettingsWindowTabKind.Annotation}">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="LanguageSectionHostStyle" TargetType="Border" BasedOn="{StaticResource SectionHostStyle}">
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Visual.SelectedSection}" Value="{x:Static settings:SettingsWindowTabKind.Language}">
                        <Setter Property="Visibility" Value="Visible" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="LightThemeChoiceButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource SettingsSecondaryButtonStyle}">
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Visual.SelectedTheme}" Value="{x:Static settings:SettingsTheme.Light}">
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="Background" Value="{DynamicResource SettingsAccentBrush}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource SettingsFocusBrush}" />
                        <Setter Property="BorderThickness" Value="2" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <Style x:Key="DarkThemeChoiceButtonStyle"
                   TargetType="Button"
                   BasedOn="{StaticResource SettingsSecondaryButtonStyle}">
                <Setter Property="HorizontalContentAlignment" Value="Left" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Visual.SelectedTheme}" Value="{x:Static settings:SettingsTheme.Dark}">
                        <Setter Property="Foreground" Value="White" />
                        <Setter Property="Background" Value="{DynamicResource SettingsAccentBrush}" />
                        <Setter Property="BorderBrush" Value="{DynamicResource SettingsFocusBrush}" />
                        <Setter Property="BorderThickness" Value="2" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter" />
            <Style x:Key="AnnotationCategoryTabStyle" TargetType="TabItem">
                <Setter Property="Margin" Value="0,0,0,8" />
                <Setter Property="Padding" Value="12,9" />
                <Setter Property="HorizontalContentAlignment" Value="Center" />
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="Foreground" Value="{DynamicResource SettingsTextSecondaryBrush}" />
                <Setter Property="FocusVisualStyle" Value="{DynamicResource SettingsFocusVisualStyle}" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabItem">
                            <Border x:Name="Chrome"
                                    Background="Transparent"
                                    BorderBrush="Transparent"
                                    BorderThickness="0,0,0,2"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter ContentSource="Header"
                                                  RecognizesAccessKey="True"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Foreground" Value="{DynamicResource SettingsAccentBrush}" />
                                    <Setter TargetName="Chrome" Property="BorderBrush" Value="{DynamicResource SettingsAccentBrush}" />
                                </Trigger>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="Chrome" Property="Background" Value="{DynamicResource SettingsHoverBrush}" />
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="AnnotationCategoryTabControlStyle" TargetType="TabControl">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="TabControl">
                            <Grid KeyboardNavigation.TabNavigation="Local">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>
                                <TabPanel Grid.Row="0"
                                          Margin="0,0,0,16"
                                          IsItemsHost="True"
                                          KeyboardNavigation.TabIndex="1" />
                                <ContentPresenter Grid.Row="1"
                                                  ContentSource="SelectedContent"
                                                  KeyboardNavigation.TabIndex="2" />
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="AnnotationScaleCardItemStyle"
                   TargetType="ListBoxItem"
                   BasedOn="{StaticResource SettingsSelectionCardItemStyle}">
                <Setter Property="Margin" Value="0,0,0,5" />
                <Setter Property="Padding" Value="8,6" />
                <Setter Property="MinWidth" Value="0" />
                <Setter Property="MinHeight" Value="42" />
                <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="{DynamicResource SettingsNavigationColumnWidth}" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.RowSpan="2"
                Grid.Column="0"
                Background="{DynamicResource SettingsSidebarBackgroundBrush}">
            <Grid Margin="0,24,0,18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="24" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <StackPanel Margin="24,0">
                    <TextBlock Foreground="{DynamicResource SettingsSidebarTextBrush}"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Text="ACAD KROVY" />
                    <TextBlock Margin="0,4,0,0"
                               Foreground="{DynamicResource SettingsSidebarMutedTextBrush}"
                               FontSize="{DynamicResource SettingsFontSizeCaption}"
                               Text="{Binding Visual.ApplicationVersionLabel}" />
                </StackPanel>

                <ListBox x:Name="SettingsNavigation"
                         Grid.Row="2"
                         ItemsSource="{Binding Visual.NavigationItems}"
                         SelectedValuePath="Section"
                         SelectedValue="{Binding Visual.SelectedSection, Mode=TwoWay}"
                         Background="Transparent"
                         BorderThickness="0"
                         ItemContainerStyle="{StaticResource SettingsNavigationItemStyle}"
                         automation:AutomationProperties.Name="{localization:Loc SettingsWindow_Accessibility_Navigation}">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="26" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Path Width="18"
                                      Height="18"
                                      Stretch="Uniform"
                                      Stroke="{DynamicResource SettingsSidebarTextBrush}"
                                      StrokeThickness="1.6">
                                    <Path.Style>
                                        <Style TargetType="Path">
                                            <Setter Property="Data" Value="{DynamicResource SettingsIconLayers}" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Section}" Value="{x:Static settings:SettingsWindowTabKind.Manufacturing}">
                                                    <Setter Property="Data" Value="{DynamicResource SettingsIconManufacturing}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Section}" Value="{x:Static settings:SettingsWindowTabKind.Annotation}">
                                                    <Setter Property="Data" Value="{DynamicResource SettingsIconAnnotation}" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding Section}" Value="{x:Static settings:SettingsWindowTabKind.Language}">
                                                    <Setter Property="Data" Value="{DynamicResource SettingsIconLanguage}" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Path.Style>
                                </Path>
                                <TextBlock Grid.Column="1"
                                           VerticalAlignment="Center"
                                           Foreground="{DynamicResource SettingsSidebarTextBrush}"
                                           TextWrapping="Wrap"
                                           Text="{Binding DisplayName}" />
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>

                <StackPanel Grid.Row="3" Margin="20,0">
                    <TextBlock Margin="4,0,0,6"
                               Foreground="{DynamicResource SettingsSidebarMutedTextBrush}"
                               FontSize="{DynamicResource SettingsFontSizeCaption}"
                               Text="{localization:Loc SettingsWindow_Theme}" />
                    <Button x:Name="LightThemeButton"
                            Margin="0,0,0,6"
                            Style="{StaticResource LightThemeChoiceButtonStyle}"
                            Click="LightTheme_Click"
                            automation:AutomationProperties.Name="{localization:Loc SettingsWindow_ThemeLight}">
                        <TextBlock Text="{localization:Loc SettingsWindow_ThemeLight}"
                                   TextWrapping="Wrap" />
                    </Button>
                    <Button x:Name="DarkThemeButton"
                            Style="{StaticResource DarkThemeChoiceButtonStyle}"
                            Click="DarkTheme_Click"
                            automation:AutomationProperties.Name="{localization:Loc SettingsWindow_ThemeDark}">
                        <TextBlock Text="{localization:Loc SettingsWindow_ThemeDark}"
                                   TextWrapping="Wrap" />
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <Grid Grid.Column="1" Grid.Row="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Grid Margin="24,20,24,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock FontSize="{DynamicResource SettingsFontSizeTitle}"
                               FontWeight="{DynamicResource SettingsFontWeightSemiBold}"
                               TextWrapping="Wrap"
                               Text="{localization:Loc SettingsWindow_Heading}" />
                    <TextBlock Margin="0,5,24,0"
                               MaxWidth="820"
                               HorizontalAlignment="Left"
                               Style="{DynamicResource SettingsDescriptionStyle}"
                               Text="{localization:Loc SettingsWindow_Description}" />
                </StackPanel>
                <Border Grid.Column="1"
                        Padding="10,5"
                        VerticalAlignment="Top"
                        Background="{DynamicResource SettingsSelectedBrush}"
                        BorderBrush="{DynamicResource SettingsAccentBrush}"
                        BorderThickness="1"
                        CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                    <TextBlock Foreground="{DynamicResource SettingsAccentBrush}"
                               FontWeight="SemiBold"
                               Text="{Binding Visual.FormStateText}" />
                </Border>
            </Grid>

            <Grid Grid.Row="1" Margin="{DynamicResource SettingsSectionMargin}">
                <Border Style="{StaticResource LayersSectionHostStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="14" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Style="{DynamicResource SettingsSectionTitleStyle}"
                                       Text="{localization:Loc SettingsWindow_Layers_Tab}" />
                            <TextBlock Margin="0,4,0,0"
                                       Style="{DynamicResource SettingsDescriptionStyle}"
                                       Text="{localization:Loc SettingsWindow_Layers_Description}" />
                        </StackPanel>
                        <DataGrid x:Name="StylesDataGrid"
                                  Grid.Row="2"
                                  Style="{DynamicResource SettingsDataGridStyle}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserReorderColumns="False"
                                  CanUserSortColumns="False"
                                  AlternationCount="2">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{localization:Loc SettingsWindow_Layers_ElementTypeColumn}"
                                                    Binding="{Binding ElementLabel}"
                                                    IsReadOnly="True"
                                                    Width="120" />
                                <DataGridTemplateColumn Header="{localization:Loc SettingsWindow_Layers_LayerNameColumn}"
                                                        Width="*">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <ComboBox IsEditable="True"
                                                      IsTextSearchEnabled="True"
                                                      IsSynchronizedWithCurrentItem="False"
                                                      StaysOpenOnEdit="True"
                                                      SelectionChanged="LayerNameComboBox_SelectionChanged"
                                                      LostKeyboardFocus="LayerNameComboBox_LostKeyboardFocus"
                                                      ItemsSource="{Binding DataContext.LayerNameOptions, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                      SelectedItem="{Binding LayerName, Mode=OneWay}"
                                                      Text="{Binding LayerName, Mode=TwoWay, UpdateSourceTrigger=LostFocus}"
                                                      automation:AutomationProperties.Name="{localization:Loc SettingsWindow_Layers_LayerNamePrompt}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="{localization:Loc SettingsWindow_Layers_LayerColorColumn}"
                                                        Width="142">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <local:AciColorPicker Margin="4,2"
                                                                  Options="{Binding DataContext.ColorOptions, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                                  SelectedAciIndex="{Binding AciColorIndex, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTemplateColumn Header="{localization:Loc SettingsWindow_Layers_LinetypeColumn}"
                                                        Width="132">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <TextBlock Margin="8,0"
                                                       VerticalAlignment="Center"
                                                       TextTrimming="CharacterEllipsis"
                                                       ToolTip="{Binding SelectedLinetypeName}"
                                                       Text="{Binding SelectedLinetypeName}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                    <DataGridTemplateColumn.CellEditingTemplate>
                                        <DataTemplate>
                                            <ComboBox ItemsSource="{Binding DataContext.LinetypeOptions, RelativeSource={RelativeSource AncestorType={x:Type Window}}}"
                                                      SelectedItem="{Binding SelectedLinetypeName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellEditingTemplate>
                                </DataGridTemplateColumn>
                                <DataGridTextColumn Header="{localization:Loc SettingsWindow_Layers_LinetypeScaleColumn}"
                                                    Binding="{Binding LinetypeScaleText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    Width="118" />
                                <DataGridTemplateColumn Header="{localization:Loc SettingsWindow_Layers_PreviewColumn}"
                                                        IsReadOnly="True"
                                                        Width="142">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Grid Margin="8,0"
                                                  ToolTip="{Binding PreviewToolTip}">
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="*" />
                                                    <RowDefinition Height="Auto" />
                                                </Grid.RowDefinitions>
                                                <Line X1="0"
                                                      X2="105"
                                                      Y1="10"
                                                      Y2="10"
                                                      Stroke="{Binding SelectedColor.Brush}"
                                                      StrokeThickness="3">
                                                    <Line.Style>
                                                        <Style TargetType="Line">
                                                            <Setter Property="StrokeDashArray" Value="" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding SelectedLinetypeName}" Value="DASHDOT">
                                                                    <Setter Property="StrokeDashArray" Value="7,3,1,3" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Line.Style>
                                                </Line>
                                                <TextBlock Grid.Row="1"
                                                           FontSize="{DynamicResource SettingsFontSizeCaption}"
                                                           Foreground="{DynamicResource SettingsTextSecondaryBrush}"
                                                           Text="{Binding LinetypeScaleText, StringFormat=× {0}}" />
                                            </Grid>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <Border Style="{StaticResource ManufacturingSectionHostStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="18" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="18" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <StackPanel>
                            <TextBlock Style="{DynamicResource SettingsSectionTitleStyle}"
                                       Text="{localization:Loc SettingsWindow_Manufacturing_Tab}" />
                            <TextBlock Margin="0,4,0,0"
                                       Style="{DynamicResource SettingsDescriptionStyle}"
                                       Text="{localization:Loc SettingsWindow_Manufacturing_Description}" />
                        </StackPanel>
                        <StackPanel Grid.Row="2" Orientation="Horizontal" VerticalAlignment="Center">
                            <TextBlock VerticalAlignment="Center"
                                       Text="{localization:Loc SettingsWindow_Manufacturing_RoundingStep}" />
                            <TextBox Width="110"
                                     Margin="14,0,8,0"
                                     Text="{Binding RoundingStepMmText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     automation:AutomationProperties.Name="{localization:Loc SettingsWindow_Manufacturing_RoundingStep}" />
                            <TextBlock VerticalAlignment="Center"
                                       Foreground="{DynamicResource SettingsTextSecondaryBrush}"
                                       Text="{localization:Loc SettingsWindow_UnitMillimetres}" />
                        </StackPanel>
                        <DataGrid x:Name="DefaultsDataGrid"
                                  Grid.Row="4"
                                  Style="{DynamicResource SettingsDataGridStyle}"
                                  AutoGenerateColumns="False"
                                  CanUserAddRows="False"
                                  CanUserDeleteRows="False"
                                  CanUserReorderColumns="False"
                                  CanUserSortColumns="False"
                                  AlternationCount="2">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="{localization:Loc SettingsWindow_Manufacturing_ElementTypeColumn}"
                                                    Binding="{Binding ElementLabel}"
                                                    IsReadOnly="True"
                                                    Width="260" />
                                <DataGridTextColumn Header="{localization:Loc SettingsWindow_Manufacturing_DefaultAllowanceColumn}"
                                                    Binding="{Binding CuttingAllowanceMmText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                    Width="*" />
                                <DataGridTextColumn Header="{localization:Loc SettingsWindow_UnitMillimetres}"
                                                    Binding="{Binding UnitLabel}"
                                                    IsReadOnly="True"
                                                    Width="90" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </Border>

                <Border Style="{StaticResource AnnotationSectionHostStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <TextBlock Style="{DynamicResource SettingsSectionTitleStyle}"
                                   Text="{localization:Loc SettingsWindow_Annotation_Tab}" />
                        <TextBlock Grid.Row="1"
                                   Margin="0,4,0,12"
                                   Style="{DynamicResource SettingsDescriptionStyle}"
                                   Text="{localization:Loc SettingsWindow_Annotation_Description}" />

                        <TabControl x:Name="AnnotationCategoryTabs"
                                    Grid.Row="2"
                                    SelectedIndex="1"
                                    Style="{StaticResource AnnotationCategoryTabControlStyle}">
                                <TabItem Header="{localization:Loc SettingsWindow_AnnotationCategory_General}"
                                         Style="{StaticResource AnnotationCategoryTabStyle}">
                                    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Auto">
                                    <ListBox x:Name="AnnotationPresetSelector"
                                             ItemsSource="{Binding AnnotationPresetOptions}"
                                             SelectedValuePath="Preset"
                                             SelectedValue="{Binding SelectedAnnotationPreset, Mode=TwoWay}"
                                             PreviewKeyDown="AnnotationPresetSelector_PreviewKeyDown"
                                             ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                             ScrollViewer.VerticalScrollBarVisibility="Disabled"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             ItemContainerStyle="{StaticResource SettingsSelectionCardItemStyle}">
                                        <ListBox.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <UniformGrid Rows="2" Columns="5" />
                                            </ItemsPanelTemplate>
                                        </ListBox.ItemsPanel>
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <Grid MinHeight="180"
                                                      ToolTip="{Binding DisplayName}"
                                                      AutomationProperties.Name="{Binding AccessibilityName}">
                                                    <Grid.RowDefinitions>
                                                        <RowDefinition Height="145" />
                                                        <RowDefinition Height="35" />
                                                    </Grid.RowDefinitions>
                                                    <Border Margin="0,0,0,5"
                                                            Background="{DynamicResource SettingsAnnotationPreviewBackgroundBrush}"
                                                            BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                            BorderThickness="1"
                                                            CornerRadius="{DynamicResource SettingsCornerRadiusSmall}"
                                                            ClipToBounds="True">
                                                        <Image Source="{Binding PreviewResourceUri}"
                                                               Stretch="Uniform"
                                                               SnapsToDevicePixels="True"
                                                               UseLayoutRounding="True" />
                                                    </Border>
                                                    <TextBlock Grid.Row="1"
                                                               HorizontalAlignment="Center"
                                                               VerticalAlignment="Center"
                                                               FontSize="{DynamicResource SettingsFontSizeCaption}"
                                                               FontWeight="SemiBold"
                                                               MaxHeight="32"
                                                               TextAlignment="Center"
                                                               TextWrapping="Wrap"
                                                               Text="{Binding DisplayName}" />
                                                    <Path Grid.RowSpan="2"
                                                          Width="13"
                                                          Height="13"
                                                          Margin="0,4,4,0"
                                                          HorizontalAlignment="Right"
                                                          VerticalAlignment="Top"
                                                          Data="{DynamicResource SettingsIconCheck}"
                                                          Stroke="{DynamicResource SettingsAccentBrush}"
                                                          StrokeThickness="2"
                                                          Stretch="Uniform">
                                                        <Path.Style>
                                                            <Style TargetType="Path">
                                                                <Setter Property="Visibility" Value="Collapsed" />
                                                                <Style.Triggers>
                                                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                                                 Value="True">
                                                                        <Setter Property="Visibility" Value="Visible" />
                                                                    </DataTrigger>
                                                                </Style.Triggers>
                                                            </Style>
                                                        </Path.Style>
                                                    </Path>
                                                </Grid>
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                    </ScrollViewer>
                                </TabItem>

                                <TabItem x:Name="AnnotationScaleTab"
                                         Header="{localization:Loc SettingsWindow_AnnotationCategory_Scale}"
                                         Style="{StaticResource AnnotationCategoryTabStyle}">
                            <Border
                                    Style="{DynamicResource SettingsCardStyle}">
                                <StackPanel>
                                    <TextBlock Style="{DynamicResource SettingsSectionTitleStyle}"
                                               Text="{localization:Loc SettingsWindow_AnnotationScale_Title}" />
                                    <Grid Margin="0,10,0,0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="270" />
                                            <ColumnDefinition Width="14" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <Border Padding="12"
                                                Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="{DynamicResource SettingsCornerRadius}">
                                            <StackPanel>
                                                <TextBlock FontWeight="SemiBold"
                                                           Text="{localization:Loc SettingsWindow_AnnotationScale_SelectedTitle}" />
                                                <TextBlock Margin="0,3,0,6"
                                                           Style="{DynamicResource SettingsDescriptionStyle}"
                                                           Text="{localization:Loc SettingsWindow_AnnotationScale_DrawingLevelHelp}" />
                                                <ListBox x:Name="DrawingAnnotationScaleSelector"
                                                         ItemsSource="{Binding AnnotationScaleOptions}"
                                                         SelectedValuePath="Preset"
                                                         SelectedValue="{Binding SelectedDrawingScalePreset, Mode=TwoWay}"
                                                         Background="Transparent"
                                                         BorderThickness="0"
                                                         ItemContainerStyle="{StaticResource AnnotationScaleCardItemStyle}"
                                                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                         ScrollViewer.VerticalScrollBarVisibility="Disabled">
                                                    <ListBox.ItemsPanel>
                                                        <ItemsPanelTemplate><StackPanel /></ItemsPanelTemplate>
                                                    </ListBox.ItemsPanel>
                                                    <ListBox.ItemTemplate>
                                                        <DataTemplate>
                                                            <RadioButton Foreground="{DynamicResource SettingsTextPrimaryBrush}"
                                                                         FontWeight="SemiBold"
                                                                         HorizontalContentAlignment="Left"
                                                                         IsChecked="{Binding IsSelected, Mode=OneWay, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                                         IsHitTestVisible="False"
                                                                        Content="{Binding DisplayName}" />
                                                        </DataTemplate>
                                                    </ListBox.ItemTemplate>
                                                </ListBox>
                                                <StackPanel Margin="0,5,0,0"
                                                            Visibility="{Binding IsDrawingCustomScale, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                    <TextBlock Text="{localization:Loc SettingsWindow_AnnotationScale_Denominator}" />
                                                    <DockPanel Margin="0,4,0,0">
                                                        <TextBlock Margin="0,0,7,0"
                                                                   VerticalAlignment="Center"
                                                                   Text="1 :" />
                                                        <TextBox x:Name="DrawingCustomScaleTextBox"
                                                                 MaxLength="3"
                                                                 Text="{Binding DrawingCustomScaleText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                    </DockPanel>
                                                    <TextBlock Margin="0,4,0,0"
                                                               Foreground="{DynamicResource SettingsValidationBrush}"
                                                               Text="{Binding ScaleValidationText}"
                                                               Visibility="{Binding HasDrawingScaleError, Converter={StaticResource BooleanToVisibilityConverter}}" />
                                                </StackPanel>
                                                <TextBlock Margin="0,8,0,0"
                                                           Style="{DynamicResource SettingsDescriptionStyle}"
                                                           Text="{Binding CurrentDrawingScaleText}" />
                                                <TextBlock Margin="0,4,0,0"
                                                           Foreground="{DynamicResource SettingsTextSecondaryBrush}"
                                                           Text="{Binding DefaultNewElementScaleText}"
                                                           TextWrapping="Wrap" />
                                            </StackPanel>
                                        </Border>

                                        <Border Grid.Column="2"
                                                Padding="12"
                                                Background="{DynamicResource SettingsAnnotationPreviewBackgroundBrush}"
                                                BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                BorderThickness="1"
                                                CornerRadius="{DynamicResource SettingsCornerRadius}">
                                            <Grid>
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="182" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>
                                            <TextBlock Foreground="{DynamicResource SettingsTextPrimaryBrush}"
                                                       FontWeight="SemiBold"
                                                       Text="{localization:Loc SettingsWindow_AnnotationScale_PreviewTitle}" />
                                            <Border Grid.Row="1"
                                                    Margin="0,8,0,8"
                                                    Background="{DynamicResource SettingsAnnotationPreviewBackgroundBrush}"
                                                    CornerRadius="{DynamicResource SettingsCornerRadiusSmall}"
                                                    ClipToBounds="True">
                                                <Image x:Name="AnnotationScalePreviewImage"
                                                       Source="{Binding SelectedAnnotationPreviewResourceUri}"
                                                       Stretch="Uniform"
                                                       SnapsToDevicePixels="True"
                                                       UseLayoutRounding="True" />
                                            </Border>
                                            <UniformGrid Grid.Row="2" Columns="2">
                                                <Border Margin="0,0,4,4"
                                                        Padding="8"
                                                        Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                        BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                    <StackPanel>
                                                    <TextBlock Foreground="{DynamicResource SettingsTextSecondaryBrush}"
                                                               Text="{localization:Loc SettingsWindow_AnnotationScale_DimensionHeight}" />
                                                    <TextBlock Margin="0,2,0,2" Foreground="{DynamicResource SettingsTextPrimaryBrush}" FontSize="16" FontWeight="SemiBold" Text="{Binding PreviewDimensionText}" />
                                                    <TextBlock FontSize="{DynamicResource SettingsFontSizeCaption}" Foreground="{DynamicResource SettingsTextSecondaryBrush}" Text="{Binding PreviewDimensionModelText}" />
                                                    <TextBlock FontSize="{DynamicResource SettingsFontSizeCaption}" Foreground="{DynamicResource SettingsTextSecondaryBrush}" Text="{Binding PreviewDimensionPaperText}" />
                                                    </StackPanel>
                                                </Border>
                                                <Border Margin="4,0,0,4"
                                                        Padding="8"
                                                        Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                        BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                    <StackPanel>
                                                    <TextBlock Foreground="{DynamicResource SettingsTextSecondaryBrush}"
                                                               Text="{localization:Loc SettingsWindow_AnnotationScale_ItemHeight}" />
                                                    <TextBlock Margin="0,2,0,2" Foreground="{DynamicResource SettingsTextPrimaryBrush}" FontSize="16" FontWeight="SemiBold" Text="{Binding PreviewItemNumberText}" />
                                                    <TextBlock FontSize="{DynamicResource SettingsFontSizeCaption}" Foreground="{DynamicResource SettingsTextSecondaryBrush}" Text="{Binding PreviewItemNumberModelText}" />
                                                    <TextBlock FontSize="{DynamicResource SettingsFontSizeCaption}" Foreground="{DynamicResource SettingsTextSecondaryBrush}" Text="{Binding PreviewItemNumberPaperText}" />
                                                    </StackPanel>
                                                </Border>
                                                <Border Margin="0,4,4,0"
                                                        Padding="8"
                                                        Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                        BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                    <StackPanel>
                                                    <TextBlock Foreground="{DynamicResource SettingsTextSecondaryBrush}"
                                                               Text="{localization:Loc SettingsWindow_AnnotationScale_SlopeHeight}" />
                                                    <TextBlock Margin="0,2,0,2" Foreground="{DynamicResource SettingsTextPrimaryBrush}" FontSize="16" FontWeight="SemiBold" Text="{Binding PreviewSlopeText}" />
                                                    <TextBlock FontSize="{DynamicResource SettingsFontSizeCaption}" Foreground="{DynamicResource SettingsTextSecondaryBrush}" Text="{Binding PreviewSlopeModelText}" />
                                                    <TextBlock FontSize="{DynamicResource SettingsFontSizeCaption}" Foreground="{DynamicResource SettingsTextSecondaryBrush}" Text="{Binding PreviewSlopePaperText}" />
                                                    </StackPanel>
                                                </Border>
                                                <Border Margin="4,4,0,0"
                                                        Padding="8"
                                                        Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                        BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                    <StackPanel>
                                                    <TextBlock Foreground="{DynamicResource SettingsTextSecondaryBrush}"
                                                               Text="{localization:Loc SettingsWindow_AnnotationScale_BlockScale}" />
                                                    <TextBlock Margin="0,2,0,2" Foreground="{DynamicResource SettingsTextPrimaryBrush}" FontSize="16" FontWeight="SemiBold" Text="{Binding PreviewBlockScale}" />
                                                    <TextBlock FontSize="{DynamicResource SettingsFontSizeCaption}" Foreground="{DynamicResource SettingsTextSecondaryBrush}" Text="{Binding PreviewBlockModelText}" />
                                                    <TextBlock FontSize="{DynamicResource SettingsFontSizeCaption}" Foreground="{DynamicResource SettingsTextSecondaryBrush}" Text="{Binding PreviewBlockPaperText}" />
                                                    </StackPanel>
                                                </Border>
                                            </UniformGrid>
                                        </Grid>
                                    </Border>
                                    </Grid>
                                </StackPanel>
                            </Border>
                                </TabItem>

                                <TabItem x:Name="AnnotationTextsTab"
                                         Header="{localization:Loc SettingsWindow_AnnotationCategory_Texts}"
                                         Style="{StaticResource AnnotationCategoryTabStyle}">
                                    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                                                  VerticalScrollBarVisibility="Auto">
                                        <Grid Margin="0,0,2,0">
                                            <Grid.RowDefinitions>
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="7" />
                                                <RowDefinition Height="Auto" />
                                                <RowDefinition Height="7" />
                                                <RowDefinition Height="Auto" />
                                            </Grid.RowDefinitions>

                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="295" MinWidth="280" MaxWidth="310" />
                                                    <ColumnDefinition Width="7" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>

                                                <Border Padding="10"
                                                        Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                        BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{DynamicResource SettingsCornerRadius}">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="*" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock FontWeight="SemiBold"
                                                                   Text="{localization:Loc SettingsWindow_AnnotationText_StylesTitle}" />
                                                        <ListBox x:Name="AnnotationTextStyleLibraryList"
                                                                 Grid.Row="1"
                                                                 Margin="0,5,0,6"
                                                                 MinHeight="142"
                                                                 MaxHeight="164"
                                                                 ItemsSource="{Binding TextStylePresetItems}"
                                                                 SelectedItem="{Binding SelectedTextStylePresetItem, Mode=TwoWay}"
                                                                 Background="Transparent"
                                                                 BorderThickness="0"
                                                                 ItemContainerStyle="{StaticResource AnnotationScaleCardItemStyle}"
                                                                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                                                                 ScrollViewer.VerticalScrollBarVisibility="Auto">
                                                            <ListBox.ItemTemplate>
                                                                <DataTemplate>
                                                                    <Grid>
                                                                        <Grid.ColumnDefinitions>
                                                                            <ColumnDefinition Width="32" />
                                                                            <ColumnDefinition Width="*" />
                                                                        </Grid.ColumnDefinitions>
                                                                        <Border Width="25"
                                                                                Height="25"
                                                                                HorizontalAlignment="Left"
                                                                                Background="{DynamicResource SettingsAnnotationPreviewBackgroundBrush}"
                                                                                BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                                                BorderThickness="1"
                                                                                CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                                            <TextBlock HorizontalAlignment="Center"
                                                                                       VerticalAlignment="Center"
                                                                                       FontFamily="{Binding FontFile}"
                                                                                       FontWeight="SemiBold"
                                                                                       Text="A" />
                                                                        </Border>
                                                                        <TextBlock Grid.Column="1"
                                                                                   VerticalAlignment="Center"
                                                                                   Foreground="{DynamicResource SettingsTextPrimaryBrush}"
                                                                                   FontWeight="SemiBold"
                                                                                   Text="{Binding DisplayName}"
                                                                                   TextTrimming="CharacterEllipsis" />
                                                                    </Grid>
                                                                </DataTemplate>
                                                            </ListBox.ItemTemplate>
                                                        </ListBox>
                                                        <UniformGrid Grid.Row="2" Columns="2">
                                                            <Button Margin="0,0,3,3"
                                                                    Padding="6,3"
                                                                    Content="{localization:Loc SettingsWindow_AnnotationText_NewStyle}"
                                                                    Click="AnnotationTextNewStyle_Click" />
                                                            <Button Margin="3,0,0,3"
                                                                    Padding="6,3"
                                                                    Content="{localization:Loc SettingsWindow_AnnotationText_EditStyle}"
                                                                    IsEnabled="{Binding CanEditSelectedTextStylePreset}"
                                                                    Click="AnnotationTextEditStyle_Click" />
                                                            <Button Margin="0,3,3,0"
                                                                    Padding="6,3"
                                                                    Content="{localization:Loc SettingsWindow_AnnotationText_RenameStyle}"
                                                                    IsEnabled="{Binding CanEditSelectedTextStylePreset}"
                                                                    Click="AnnotationTextRenameStyle_Click" />
                                                            <Button Margin="3,3,0,0"
                                                                    Padding="6,3"
                                                                    Content="{localization:Loc SettingsWindow_AnnotationText_DeleteStyle}"
                                                                    IsEnabled="{Binding CanEditSelectedTextStylePreset}"
                                                                    Click="AnnotationTextDeleteStyle_Click" />
                                                        </UniformGrid>
                                                    </Grid>
                                                </Border>

                                                <Border Grid.Column="2"
                                                        Padding="10"
                                                        Background="{DynamicResource SettingsAnnotationPreviewBackgroundBrush}"
                                                        BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{DynamicResource SettingsCornerRadius}">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="*" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock FontWeight="SemiBold"
                                                                   Text="{localization:Loc SettingsWindow_AnnotationText_SelectedStylePreview}" />
                                                        <UniformGrid Grid.Row="1" Margin="0,6,0,5" Columns="3">
                                                            <StackPanel HorizontalAlignment="Center">
                                                                <TextBlock HorizontalAlignment="Center"
                                                                           FontSize="26"
                                                                           FontWeight="{Binding SelectedTextStylePreviewFontWeight}"
                                                                           FontFamily="{Binding SelectedTextStylePreviewFontFamily}"
                                                                           Text="K1" />
                                                                <TextBlock Margin="0,1,0,0"
                                                                           HorizontalAlignment="Center"
                                                                           Style="{DynamicResource SettingsDescriptionStyle}"
                                                                           Text="{localization:Loc SettingsWindow_AnnotationText_ItemCodeRole}" />
                                                            </StackPanel>
                                                            <StackPanel HorizontalAlignment="Center">
                                                                <TextBlock HorizontalAlignment="Center"
                                                                           FontSize="26"
                                                                           FontWeight="{Binding SelectedTextStylePreviewFontWeight}"
                                                                           FontFamily="{Binding SelectedTextStylePreviewFontFamily}"
                                                                           Text="80/160" />
                                                                <TextBlock Margin="0,1,0,0"
                                                                           HorizontalAlignment="Center"
                                                                           Style="{DynamicResource SettingsDescriptionStyle}"
                                                                           Text="{localization:Loc SettingsWindow_AnnotationText_DimensionRole}" />
                                                            </StackPanel>
                                                            <StackPanel HorizontalAlignment="Center">
                                                                <TextBlock HorizontalAlignment="Center"
                                                                           FontSize="26"
                                                                           FontWeight="{Binding SelectedTextStylePreviewFontWeight}"
                                                                           FontFamily="{Binding SelectedTextStylePreviewFontFamily}"
                                                                           Text="35°" />
                                                                <TextBlock Margin="0,1,0,0"
                                                                           HorizontalAlignment="Center"
                                                                           Style="{DynamicResource SettingsDescriptionStyle}"
                                                                           Text="{localization:Loc SettingsWindow_AnnotationText_SlopeRole}" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                        <UniformGrid Grid.Row="2" Columns="3">
                                                            <StackPanel Margin="0,0,8,0">
                                                                <TextBlock FontWeight="SemiBold"
                                                                           Style="{DynamicResource SettingsDescriptionStyle}"
                                                                           Text="{localization:Loc SettingsWindow_AnnotationText_AutoCadStyle}" />
                                                                <TextBlock Text="{Binding SelectedTextStyleAutoCadName}"
                                                                           TextTrimming="CharacterEllipsis" />
                                                            </StackPanel>
                                                            <StackPanel Margin="4,0">
                                                                <TextBlock FontWeight="SemiBold"
                                                                           Style="{DynamicResource SettingsDescriptionStyle}"
                                                                           Text="{localization:Loc SettingsWindow_AnnotationText_Font}" />
                                                                <TextBlock Text="{Binding SelectedTextStyleFontName}"
                                                                           TextTrimming="CharacterEllipsis" />
                                                            </StackPanel>
                                                            <StackPanel Margin="8,0,0,0">
                                                                <TextBlock FontWeight="SemiBold"
                                                                           Style="{DynamicResource SettingsDescriptionStyle}"
                                                                           Text="{localization:Loc SettingsWindow_AnnotationText_Type}" />
                                                                <TextBlock Text="{Binding SelectedTextStyleType}"
                                                                           TextTrimming="CharacterEllipsis" />
                                                            </StackPanel>
                                                        </UniformGrid>
                                                        <TextBlock Grid.Row="3"
                                                                   Margin="0,4,0,0"
                                                                   Style="{DynamicResource SettingsDescriptionStyle}"
                                                                   Text="{Binding SelectedTextStylePreviewStatus}"
                                                                   TextWrapping="Wrap"
                                                                   Visibility="{Binding IsApproximatePreview, Converter={StaticResource BooleanToVisibilityConverter}}" />

                                                        <Border Grid.RowSpan="4"
                                                                Padding="10"
                                                                Background="{DynamicResource SettingsAnnotationPreviewBackgroundBrush}"
                                                                BorderBrush="{DynamicResource SettingsAccentBrush}"
                                                                BorderThickness="1"
                                                                CornerRadius="{DynamicResource SettingsCornerRadiusSmall}"
                                                                Visibility="{Binding IsTextStyleEditorVisible, Converter={StaticResource BooleanToVisibilityConverter}}">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*" />
                                                                    <ColumnDefinition Width="12" />
                                                                    <ColumnDefinition Width="150" />
                                                                </Grid.ColumnDefinitions>
                                                                <Grid>
                                                                    <Grid.RowDefinitions>
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                        <RowDefinition Height="Auto" />
                                                                    </Grid.RowDefinitions>
                                                                    <Grid.ColumnDefinitions>
                                                                        <ColumnDefinition Width="116" />
                                                                        <ColumnDefinition Width="*" />
                                                                    </Grid.ColumnDefinitions>
                                                                    <TextBlock VerticalAlignment="Center" Text="{localization:Loc SettingsWindow_AnnotationText_StyleName}" />
                                                                    <TextBox Grid.Column="1" Margin="0,0,0,5" Text="{Binding TextStyleEditorName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                                    <TextBlock Grid.Row="1" VerticalAlignment="Center" Text="{localization:Loc SettingsWindow_AnnotationText_Font}" />
                                                                    <ComboBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,5" ItemsSource="{Binding AvailableFontNames}" SelectedItem="{Binding TextStyleEditorFont, Mode=TwoWay}" IsEditable="False" />
                                                                    <TextBlock Grid.Row="2" VerticalAlignment="Center" Text="{localization:Loc SettingsWindow_AnnotationText_WidthFactor}" />
                                                                    <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,5" Text="{Binding TextStyleEditorWidthFactorText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                                    <TextBlock Grid.Row="3" VerticalAlignment="Center" Text="{localization:Loc SettingsWindow_AnnotationText_ObliqueAngle}" />
                                                                    <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,5" Text="{Binding TextStyleEditorObliqueAngleText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                                    <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal">
                                                                        <Button MinWidth="82" Padding="10,4" Content="{localization:Loc SettingsWindow_AnnotationText_SaveStyle}" Click="AnnotationTextSaveStyle_Click" />
                                                                        <Button Margin="7,0,0,0" MinWidth="82" Padding="10,4" Content="{localization:Loc SettingsWindow_AnnotationText_CancelStyle}" Click="AnnotationTextCancelStyle_Click" />
                                                                    </StackPanel>
                                                                </Grid>
                                                                <StackPanel Grid.Column="2">
                                                                    <TextBlock FontWeight="SemiBold" Text="{localization:Loc SettingsWindow_AnnotationText_Preview}" />
                                                                    <TextBlock Margin="0,12,0,0"
                                                                               FontSize="26"
                                                                               FontWeight="SemiBold"
                                                                               Text="{Binding TextStyleEditorPreviewSample}"
                                                                               FontFamily="{Binding TextStyleEditorPreviewFontFamily}" />
                                                                </StackPanel>
                                                            </Grid>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </Grid>

                                            <Grid Grid.Row="2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="7" />
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="7" />
                                                    <ColumnDefinition Width="*" />
                                                </Grid.ColumnDefinitions>
                                                <Border MinHeight="236"
                                                        Padding="10"
                                                        Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                        BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{DynamicResource SettingsCornerRadius}">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" />
                                                            <RowDefinition Height="*" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock FontWeight="SemiBold" Text="{localization:Loc SettingsWindow_AnnotationText_ItemCodeRole}" />
                                                        <TextBlock Grid.Row="1" Margin="0,5,0,2" Style="{DynamicResource SettingsDescriptionStyle}" Text="{localization:Loc SettingsWindow_AnnotationText_Style}" />
                                                        <ComboBox Grid.Row="2" ItemsSource="{Binding TextStyleKindOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Kind" SelectedValue="{Binding ItemCodeStyleKind, Mode=TwoWay}" />
                                                        <Grid Grid.Row="3" Margin="0,4,0,0">
                                                            <Grid.Style>
                                                                <Style TargetType="Grid">
                                                                    <Setter Property="Visibility" Value="Hidden" />
                                                                    <Style.Triggers><DataTrigger Binding="{Binding IsItemCodeCustomStyleVisible}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                                                                </Style>
                                                            </Grid.Style>
                                                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                                            <TextBlock Style="{DynamicResource SettingsDescriptionStyle}" Text="{localization:Loc SettingsWindow_AnnotationText_AutoCadStyle}" />
                                                            <ComboBox Grid.Row="1" Margin="0,3,0,0" ItemsSource="{Binding CustomTextStylePresetItems}" DisplayMemberPath="DisplayName" SelectedValuePath="AutoCadTextStyleName" SelectedValue="{Binding ItemCodeCustomStyleName, Mode=TwoWay}" />
                                                        </Grid>
                                                        <TextBlock Grid.Row="4" Margin="0,5,0,2" Text="{localization:Loc SettingsWindow_AnnotationText_PaperHeight}" />
                                                        <TextBox Grid.Row="5" Text="{Binding ItemCodePaperHeightText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                        <Grid Grid.Row="6" Margin="0,4,0,0">
                                                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                                            <TextBlock Style="{DynamicResource SettingsDescriptionStyle}" Text="{Binding ItemCodeModelHeightText}" />
                                                            <Border Grid.Row="1" Margin="0,4,0,0" MinHeight="48" Padding="6" Background="{DynamicResource SettingsAnnotationPreviewBackgroundBrush}" CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="21" FontWeight="{Binding ItemCodePreviewFontWeight}" Text="{Binding ItemCodePreviewSample}" FontFamily="{Binding ItemCodePreviewFontFamily}" />
                                                            </Border>
                                                        </Grid>
                                                    </Grid>
                                                </Border>

                                                <Border Grid.Column="2"
                                                        MinHeight="236"
                                                        Padding="10"
                                                        Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                        BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{DynamicResource SettingsCornerRadius}">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock FontWeight="SemiBold" Text="{localization:Loc SettingsWindow_AnnotationText_DimensionRole}" />
                                                        <TextBlock Grid.Row="1" Margin="0,5,0,2" Style="{DynamicResource SettingsDescriptionStyle}" Text="{localization:Loc SettingsWindow_AnnotationText_Style}" />
                                                        <ComboBox Grid.Row="2" ItemsSource="{Binding TextStyleKindOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Kind" SelectedValue="{Binding DimensionStyleKind, Mode=TwoWay}" />
                                                        <Grid Grid.Row="3" Margin="0,4,0,0">
                                                            <Grid.Style>
                                                                <Style TargetType="Grid">
                                                                    <Setter Property="Visibility" Value="Hidden" />
                                                                    <Style.Triggers><DataTrigger Binding="{Binding IsDimensionCustomStyleVisible}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                                                                </Style>
                                                            </Grid.Style>
                                                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                                            <TextBlock Style="{DynamicResource SettingsDescriptionStyle}" Text="{localization:Loc SettingsWindow_AnnotationText_AutoCadStyle}" />
                                                            <ComboBox Grid.Row="1" Margin="0,3,0,0" ItemsSource="{Binding CustomTextStylePresetItems}" DisplayMemberPath="DisplayName" SelectedValuePath="AutoCadTextStyleName" SelectedValue="{Binding DimensionCustomStyleName, Mode=TwoWay}" />
                                                        </Grid>
                                                        <TextBlock Grid.Row="4" Margin="0,5,0,2" Text="{localization:Loc SettingsWindow_AnnotationText_PaperHeight}" />
                                                        <TextBox Grid.Row="5" Text="{Binding DimensionPaperHeightText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                        <Grid Grid.Row="6" Margin="0,4,0,0">
                                                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                                            <TextBlock Style="{DynamicResource SettingsDescriptionStyle}" Text="{Binding DimensionModelHeightText}" />
                                                            <Border Grid.Row="1" Margin="0,4,0,0" MinHeight="48" Padding="6" Background="{DynamicResource SettingsAnnotationPreviewBackgroundBrush}" CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="21" FontWeight="{Binding DimensionPreviewFontWeight}" Text="{Binding DimensionPreviewSample}" FontFamily="{Binding DimensionPreviewFontFamily}" />
                                                            </Border>
                                                        </Grid>
                                                    </Grid>
                                                </Border>

                                                <Border Grid.Column="4"
                                                        MinHeight="236"
                                                        Padding="10"
                                                        Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                        BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                        BorderThickness="1"
                                                        CornerRadius="{DynamicResource SettingsCornerRadius}">
                                                    <Grid>
                                                        <Grid.RowDefinitions>
                                                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" />
                                                            <RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /><RowDefinition Height="*" />
                                                        </Grid.RowDefinitions>
                                                        <TextBlock FontWeight="SemiBold" Text="{localization:Loc SettingsWindow_AnnotationText_SlopeRole}" />
                                                        <TextBlock Grid.Row="1" Margin="0,5,0,2" Style="{DynamicResource SettingsDescriptionStyle}" Text="{localization:Loc SettingsWindow_AnnotationText_Style}" />
                                                        <ComboBox Grid.Row="2" ItemsSource="{Binding TextStyleKindOptions}" DisplayMemberPath="DisplayName" SelectedValuePath="Kind" SelectedValue="{Binding SlopeStyleKind, Mode=TwoWay}" />
                                                        <Grid Grid.Row="3" Margin="0,4,0,0">
                                                            <Grid.Style>
                                                                <Style TargetType="Grid">
                                                                    <Setter Property="Visibility" Value="Hidden" />
                                                                    <Style.Triggers><DataTrigger Binding="{Binding IsSlopeCustomStyleVisible}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger></Style.Triggers>
                                                                </Style>
                                                            </Grid.Style>
                                                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
                                                            <TextBlock Style="{DynamicResource SettingsDescriptionStyle}" Text="{localization:Loc SettingsWindow_AnnotationText_AutoCadStyle}" />
                                                            <ComboBox Grid.Row="1" Margin="0,3,0,0" ItemsSource="{Binding CustomTextStylePresetItems}" DisplayMemberPath="DisplayName" SelectedValuePath="AutoCadTextStyleName" SelectedValue="{Binding SlopeCustomStyleName, Mode=TwoWay}" />
                                                        </Grid>
                                                        <TextBlock Grid.Row="4" Margin="0,5,0,2" Text="{localization:Loc SettingsWindow_AnnotationText_PaperHeight}" />
                                                        <TextBox Grid.Row="5" Text="{Binding SlopePaperHeightText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                                                        <Grid Grid.Row="6" Margin="0,4,0,0">
                                                            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /></Grid.RowDefinitions>
                                                            <TextBlock Style="{DynamicResource SettingsDescriptionStyle}" Text="{Binding SlopeModelHeightText}" />
                                                            <Border Grid.Row="1" Margin="0,4,0,0" MinHeight="48" Padding="6" Background="{DynamicResource SettingsAnnotationPreviewBackgroundBrush}" CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="21" FontWeight="{Binding SlopePreviewFontWeight}" Text="{Binding SlopePreviewSample}" FontFamily="{Binding SlopePreviewFontFamily}" />
                                                            </Border>
                                                        </Grid>
                                                    </Grid>
                                                </Border>
                                            </Grid>

                                            <Border Grid.Row="4"
                                                    Padding="8,6"
                                                    Background="{DynamicResource SettingsPanelBackgroundBrush}"
                                                    BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                <TextBlock Style="{DynamicResource SettingsDescriptionStyle}"
                                                           TextWrapping="Wrap"
                                                           Text="{localization:Loc SettingsWindow_AnnotationText_BlocksNote}" />
                                            </Border>
                                        </Grid>
                                    </ScrollViewer>
                                </TabItem>
                                <TabItem Header="{localization:Loc SettingsWindow_AnnotationCategory_LineElements}"
                                         Style="{StaticResource AnnotationCategoryTabStyle}">
                                    <Border Style="{DynamicResource SettingsCardStyle}">
                                        <TextBlock HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Style="{DynamicResource SettingsDescriptionStyle}"
                                                   Text="{localization:Loc SettingsWindow_AnnotationCategory_ComingSoon}" />
                                    </Border>
                                </TabItem>
                                <TabItem Header="{localization:Loc SettingsWindow_AnnotationCategory_PostElements}"
                                         Style="{StaticResource AnnotationCategoryTabStyle}">
                                    <Border Style="{DynamicResource SettingsCardStyle}">
                                        <TextBlock HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Style="{DynamicResource SettingsDescriptionStyle}"
                                                   Text="{localization:Loc SettingsWindow_AnnotationCategory_ComingSoon}" />
                                    </Border>
                                </TabItem>
                                <TabItem Header="{localization:Loc SettingsWindow_AnnotationCategory_Tags}"
                                         Style="{StaticResource AnnotationCategoryTabStyle}">
                                    <Border Style="{DynamicResource SettingsCardStyle}">
                                        <TextBlock HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   Style="{DynamicResource SettingsDescriptionStyle}"
                                                   Text="{localization:Loc SettingsWindow_AnnotationCategory_ComingSoon}" />
                                    </Border>
                                </TabItem>
                        </TabControl>
                    </Grid>
                </Border>

                <Border Style="{StaticResource LanguageSectionHostStyle}">
                    <ScrollViewer VerticalScrollBarVisibility="Auto">
                        <StackPanel>
                            <TextBlock Style="{DynamicResource SettingsSectionTitleStyle}"
                                       Text="{localization:Loc SettingsWindow_Language_Tab}" />
                            <TextBlock Margin="0,4,0,18"
                                       Style="{DynamicResource SettingsDescriptionStyle}"
                                       Text="{localization:Loc SettingsWindow_Language_Description}" />
                            <ListBox x:Name="LanguageSelector"
                                     ItemsSource="{Binding LanguageOptions}"
                                     SelectedValuePath="Code"
                                     SelectedValue="{Binding SelectedLanguageCode, Mode=OneWay}"
                                     SelectionChanged="LanguageSelector_SelectionChanged"
                                     Background="Transparent"
                                     BorderThickness="0"
                                     ItemContainerStyle="{StaticResource SettingsLanguageCardItemStyle}">
                                <ListBox.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <UniformGrid Rows="2" Columns="3" />
                                    </ItemsPanelTemplate>
                                </ListBox.ItemsPanel>
                                <ListBox.ItemTemplate>
                                    <DataTemplate>
                                        <Grid MinHeight="220"
                                              HorizontalAlignment="Stretch"
                                              VerticalAlignment="Stretch"
                                              ClipToBounds="True"
                                              AutomationProperties.Name="{Binding AccessibilityName}">
                                            <Border HorizontalAlignment="Stretch"
                                                    VerticalAlignment="Stretch"
                                                    Background="{DynamicResource SettingsFlagCardBackgroundBrush}"
                                                    BorderBrush="{DynamicResource SettingsBorderBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="{DynamicResource SettingsCornerRadiusSmall}"
                                                    ClipToBounds="True"
                                                    IsHitTestVisible="False">
                                                <Image HorizontalAlignment="Stretch"
                                                       VerticalAlignment="Stretch"
                                                       Stretch="Fill"
                                                       SnapsToDevicePixels="True"
                                                       UseLayoutRounding="True">
                                                    <Image.Style>
                                                        <Style TargetType="Image">
                                                            <Setter Property="Source" Value="{Binding BlackFlagUri}" />
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                                             Value="True">
                                                                    <Setter Property="Source" Value="{Binding ColorFlagUri}" />
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Image.Style>
                                                </Image>
                                            </Border>
                                            <Border Width="38"
                                                    Height="28"
                                                    Margin="10"
                                                    HorizontalAlignment="Left"
                                                    VerticalAlignment="Bottom"
                                                    Panel.ZIndex="2"
                                                    Background="{DynamicResource SettingsBadgeBackgroundBrush}"
                                                    BorderBrush="{DynamicResource SettingsAccentBrush}"
                                                    BorderThickness="1"
                                                    CornerRadius="{DynamicResource SettingsCornerRadiusSmall}">
                                                <TextBlock HorizontalAlignment="Center"
                                                           VerticalAlignment="Center"
                                                           Foreground="{DynamicResource SettingsBadgeTextBrush}"
                                                           FontWeight="SemiBold"
                                                           Text="{Binding UpperCode}" />
                                            </Border>
                                            <Path Width="14"
                                                  Height="14"
                                                  Margin="10"
                                                  HorizontalAlignment="Right"
                                                  VerticalAlignment="Top"
                                                  Panel.ZIndex="3"
                                                  Data="{DynamicResource SettingsIconCheck}"
                                                  Stroke="{DynamicResource SettingsAccentBrush}"
                                                  StrokeThickness="2"
                                                  Stretch="Uniform">
                                                <Path.Style>
                                                    <Style TargetType="Path">
                                                        <Setter Property="Visibility" Value="Collapsed" />
                                                        <Style.Triggers>
                                                            <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}"
                                                                         Value="True">
                                                                <Setter Property="Visibility" Value="Visible" />
                                                            </DataTrigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Path.Style>
                                            </Path>
                                        </Grid>
                                    </DataTemplate>
                                </ListBox.ItemTemplate>
                            </ListBox>
                        </StackPanel>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Grid>

        <Border Grid.Column="1"
                Grid.Row="1"
                Padding="24,14"
                Background="{DynamicResource SettingsPanelBackgroundBrush}"
                BorderBrush="{DynamicResource SettingsBorderBrush}"
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button x:Name="RestoreDefaultsButton"
                        Style="{DynamicResource SettingsSecondaryButtonStyle}"
                        Click="RestoreDefaults_Click"
                        Content="{localization:Loc SettingsWindow_RestoreDefaults}" />

                <StackPanel x:Name="LayersFooterActions"
                            Grid.Column="2"
                            Visibility="Collapsed"
                            Orientation="Horizontal">
                    <Button x:Name="LayersApplyButton"
                            Margin="0,0,10,0"
                            Style="{DynamicResource SettingsPrimaryButtonStyle}"
                            Click="Apply_Click"
                            Content="{localization:Loc SettingsWindow_Apply}" />
                    <Button Style="{DynamicResource SettingsSecondaryButtonStyle}"
                            Click="Close_Click"
                            IsCancel="True"
                            Content="{localization:Loc SettingsWindow_Close}" />
                </StackPanel>

                <StackPanel x:Name="ManufacturingFooterActions"
                            Grid.Column="2"
                            Visibility="Collapsed"
                            Orientation="Horizontal">
                    <Button x:Name="ManufacturingApplyButton"
                            Margin="0,0,10,0"
                            Style="{DynamicResource SettingsPrimaryButtonStyle}"
                            Click="Apply_Click"
                            Content="{localization:Loc SettingsWindow_Apply}" />
                    <Button Style="{DynamicResource SettingsSecondaryButtonStyle}"
                            Click="Close_Click"
                            IsCancel="True"
                            Content="{localization:Loc SettingsWindow_Close}" />
                </StackPanel>

                <StackPanel x:Name="AnnotationFooterActions"
                            Grid.Column="2"
                            Visibility="Collapsed"
                            Orientation="Horizontal">
                    <Button x:Name="SaveNewElementsButton"
                            Margin="0,0,10,0"
                            Style="{DynamicResource SaveNewActionButtonStyle}"
                            Click="SaveNewElements_Click">
                        <TextBlock Text="{localization:Loc SettingsWindow_SaveNewElementsOnly}"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap" />
                    </Button>
                    <Button x:Name="SaveApplySelectionButton"
                            Margin="0,0,10,0"
                            Style="{DynamicResource ApplySelectionActionButtonStyle}"
                            Click="SaveApplySelection_Click">
                        <TextBlock Text="{localization:Loc SettingsWindow_SaveApplySelection}"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap" />
                    </Button>
                    <Button x:Name="SaveApplyAllButton"
                            Margin="0,0,10,0"
                            Style="{DynamicResource ApplyAllActionButtonStyle}"
                            Click="SaveApplyAll_Click">
                        <TextBlock Text="{localization:Loc SettingsWindow_SaveApplyAll}"
                                   TextAlignment="Center"
                                   TextWrapping="Wrap" />
                    </Button>
                    <Button Style="{DynamicResource SettingsSecondaryButtonStyle}"
                            Click="Close_Click"
                            IsCancel="True"
                            Content="{localization:Loc SettingsWindow_Close}" />
                </StackPanel>

                <StackPanel x:Name="LanguageFooterActions"
                            Grid.Column="2"
                            Visibility="Collapsed"
                            Orientation="Horizontal">
                    <Button Style="{DynamicResource SettingsSecondaryButtonStyle}"
                            Click="Close_Click"
                            IsCancel="True"
                            Content="{localization:Loc SettingsWindow_Close}" />
                </StackPanel>
            </Grid>
        </Border>

        <Border x:Name="StatusBanner"
                Grid.ColumnSpan="2"
                Grid.RowSpan="2"
                Panel.ZIndex="100"
                MaxWidth="860"
                Margin="44"
                Padding="26,19"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Background="{DynamicResource SettingsSuccessBackgroundBrush}"
                BorderBrush="{DynamicResource SettingsSuccessBorderBrush}"
                BorderThickness="1"
                CornerRadius="{DynamicResource SettingsCornerRadiusLarge}"
                Effect="{DynamicResource SettingsCardElevation}"
                IsHitTestVisible="False"
                Visibility="Collapsed"
                automation:AutomationProperties.LiveSetting="Polite">
            <TextBlock x:Name="StatusBannerText"
                       FontSize="19"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap" />
        </Border>
    </Grid>
</Window>
